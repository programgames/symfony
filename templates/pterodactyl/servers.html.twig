{% extends 'base.html.twig' %}

{% block title %}Serveurs Minecraft{% endblock %}

{% block body %}
    <div class="container py-5">
        <h1 class="mb-4 text-center">🎮 Serveurs Minecraft Disponibles</h1>

        <div class="table-responsive">
            <table class="table table-hover table-striped align-middle">
                <thead class="table-dark">
                <tr>
                    <th scope="col">Statut</th>
                    <th scope="col">Nom du Serveur</th>
                    <th scope="col">Mémoire (MB)</th>
                    <th scope="col">Port</th>
                    <th scope="col">URL de Connexion</th>
                </tr>
                </thead>
                <tbody>
                {% for server in data %}
                    <tr>
                        <td>
                            {% if server.state == 'offline' %}
                                <span class="badge bg-danger">Suspendu</span>
                            {% else %}
                                <span class="badge bg-success">Actif</span>
                            {% endif %}
                        </td>
                        <td>
                            <strong>{{ server.name }}</strong>
                        </td>
                        <td>
                            <span class="badge bg-info text-dark">{{ server.memoryLimit }} MB</span>
                        </td>
                        <td>
                            <code class="text-muted">{{ server.port }}</code>
                        </td>
                        <td>
                            <span class="text-primary fw-bold">{{ server.url }}</span>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="text-center text-muted">
                            Aucun serveur disponible
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>

        <div class="alert alert-info mt-4" role="alert">
            <strong>💡 Info :</strong> Pour vous connecter, copiez l'URL de connexion dans votre client Minecraft.
        </div>
    </div>

    <style>
        .table {
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
        }

        .table thead th {
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.875rem;
            letter-spacing: 0.5px;
        }

        tbody tr {
            transition: all 0.3s ease;
        }

        tbody tr:hover {
            transform: scale(1.01);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        code {
            background-color: #f8f9fa;
            padding: 2px 6px;
            border-radius: 4px;
        }
    </style>
{% endblock %}
